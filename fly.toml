# fly.toml app configuration file generated for mtg-semantic-search on 2025-10-06T23:02:53-07:00
#
# See https://fly.io/docs/reference/configuration/ for information about how to use this file.
#

app = 'mtg-semantic-search'
primary_region = 'sjc'

[build]
dockerfile = 'deploy/Dockerfile'

[http_service]
internal_port = 5000
force_https = true
auto_stop_machines = 'stop'
auto_start_machines = true
min_machines_running = 0
processes = ['app']

[[vm]]
memory = '1gb'
cpu_kind = 'shared'
cpus = 1
